# Scriptorium 2.0 리워크 계획

## 요약

Scriptorium을 RisuAI 전용 Obsidian 플러그인으로 처음부터 다시 구현합니다.

- 여러 프로젝트 폴더를 등록할 수 있지만 한 번에 활성 문서가 속한 프로젝트 하나만 처리합니다.
- 프로젝트 중첩은 허용하지 않습니다.
- 대시보드가 활성 문서를 따라가며, 프로젝트 밖의 문서에서는 `프로젝트 없음` 상태가 됩니다.
- 원문 Markdown과 `translate/` 번역본을 기준으로 문단별 변경을 추적합니다.
- JSON 파일 생성은 선택 기능이며, 평소 RisuAI 동기화에는 필요하지 않습니다.
- 데스크톱은 로컬 서버와 릴레이를 모두 지원하고, 모바일은 릴레이만 지원합니다.
- 프로젝트 내부에는 도구 설정 파일을 새로 만들지 않습니다.

## 핵심 구현

### 프로젝트와 설정

- 명령 팔레트의 `프로젝트 등록`이 폴더 선택 창을 열고 정확한 프로젝트 루트를 등록합니다.
- 등록된 프로젝트끼리 경로가 겹치거나 중첩되면 등록을 거부합니다.
- 마지막으로 포커스된 파일이 등록 루트 안에 있으면 해당 프로젝트를 활성화합니다.
- 대시보드를 클릭한 것은 파일 전환으로 간주하지 않지만, 프로젝트 밖의 다른 파일을 열면 즉시 비활성화합니다.
- 프로젝트 설정은 `id`, 이름, 루트 경로, 원문/번역 동기화 모드, 제외 경로 패턴만 가집니다.
- 전역 설정은 공용 번역 프롬프트, OpenAI 호환 API 주소·모델, 로컬 서버, 릴레이 설정을 가집니다.
- API 키와 릴레이 토큰은 Obsidian `SecretStorage`에 저장하고 설정에는 비밀값 이름만 기록합니다. 최소 Obsidian 버전은 1.11.4로 올립니다. ([공식 SecretStorage 가이드](https://docs.obsidian.md/plugins/guides/secret-storage))
- 프로젝트 제외 규칙은 대시보드 설정의 glob 목록과 문서 frontmatter의 `scriptorium: false`를 함께 지원합니다.

프로젝트 폴더에 남는 것은 다음뿐입니다.

```text
project/
  원문 Markdown
  translate/
  risu_lorebook.json   # 사용자가 내보냈을 때만 존재
```

프롬프트, 제외 규칙, 프로젝트 설정, 번역 캐시, 선택 상태는 플러그인 내부에 저장합니다.

### Git 스타일 대시보드

오른쪽 사이드바 `ItemView`로 구현하고 별도 UI 프레임워크는 사용하지 않습니다.

- 헤더: 활성 프로젝트, 동기화 모드, 변경·충돌 수, 로컬 서버와 릴레이 상태
- `변경 사항` 접이식 패널: 파일별 변경 문단, 선택/해제, 전체 선택, 이전 원문과 현재 원문 diff
- `번역할 변경 사항` 영역: Git의 staged changes처럼 선택된 문단을 별도 표시
- 선택 상태는 대시보드를 닫거나 Obsidian을 재시작해도 유지
- 충돌 영역: 원문과 번역본이 동시에 수정된 문단 표시
- 작업 영역: 현재 파일·문단, 스트리밍 출력, 성공·실패 수, 취소
- 보조 작업: JSON 내보내기, 릴레이 즉시 갱신, 프로젝트 설정
- 고급 도구: 캐시 재구축, 기존 번역본 다시 채택, 선택 상태 초기화, 프로젝트 등록 해제

선택 전용 모달과 편집기 선택 영역 번역은 만들지 않습니다.

### 문단 변경과 부분 번역

- YAML frontmatter는 본문과 분리합니다.
- 본문은 빈 줄을 기본 경계로 나눕니다.
- 제목, 연속 목록, 표, 인용문, fenced code block은 각각 하나의 독립 블록으로 처리합니다.
- 이전 원문 스냅샷과 현재 블록 배열을 비교해 삽입·수정·삭제·이동을 판단합니다.
- 내용이 같은 이동 블록은 재번역하지 않고 기존 번역을 이동합니다.
- 분할·병합처럼 대응이 불확실한 인접 블록은 하나의 변경 묶음으로 처리합니다.
- 각 변경 묶음에는 상위 제목 경로와 앞뒤 변경되지 않은 블록 하나씩을 읽기 전용 문맥으로 제공합니다.
- 한 요청은 같은 파일의 연속 변경 블록을 묶으며 12,000자를 넘기기 전에 나눕니다. 단일 블록은 중간에서 자르지 않습니다.
- global prompt는 system 메시지로 사용하고, 블록 ID와 반환 형식은 플러그인의 고정 프로토콜로 추가합니다.
- 스트리밍을 우선 사용하고 지원되지 않으면 동일 요청을 일반 응답으로 한 번 재시도합니다.
- 응답 블록 ID가 누락되거나 파싱되지 않으면 한 번 복구 요청 후 실패로 남깁니다.
- 번역 요청은 순차 실행하고, 한 묶음이 실패해도 나머지는 계속 처리합니다.
- 취소 시 완료된 문단은 유지하고 미완료·실패 문단은 선택 상태를 유지합니다.

`번역 실행`의 범위는 다음과 같습니다.

1. 선택된 변경 문단이 있으면 선택된 항목만 처리
2. 선택된 항목이 없으면 감지된 모든 변경 문단 처리
3. JSON 내보내기나 수동 동기화는 실행하지 않음
4. 릴레이 자동 갱신이 켜져 있으면 결과 변경 후 자동 전송

번역본 직접 수정은 허용합니다. 캐시는 마지막 원문, 마지막 자동 생성 번역, 현재 번역을 비교합니다.

- 원문만 변경: 부분 번역 가능
- 번역본만 변경: 사용자 수정으로 채택
- 같은 문단의 양쪽이 변경: 충돌로 표시하고 자동 패치 금지
- 충돌 해결: `수동 번역 유지`, `AI 번역으로 교체`, `문서 열기` 제공
- 원문 문단 삭제를 실행 대상으로 선택하면 대응 번역 문단도 제거
- 원문 파일 삭제 시 번역 파일은 자동 삭제하지 않지만 빌드·동기화에서 제외
- 원문 파일 이름 변경은 충돌이 없을 때 대응 번역 파일과 캐시 경로를 함께 이동

frontmatter는 파일에 자동 삽입하지 않습니다.

- `title`: 파일명에서 추론
- `keys`: 기본적으로 파일명 하나
- 나머지 Risu 메타데이터: 기존 기본값 사용
- 문서 frontmatter가 있으면 명시된 값으로 덮어쓰기
- 번역 대상 메타데이터는 `keys`만 사용하며 원문 키를 유지한 채 번역 키를 중복 없이 추가
- `keys` 변경은 대시보드에 별도 메타데이터 변경 항목으로 표시

### 로어북 생성과 동기화

하나의 Risu 컴파일러가 원문 모드, 번역 모드, JSON 내보내기, 로컬 서버, 릴레이에서 공통으로 사용됩니다.

- 출력 로어북 형식은 `{ "type": "risu", "ver": 1, "data": [...] }`
- frontmatter가 없는 문서도 추론된 기본값으로 포함
- 번역 모드에서는 번역 본문, 번역된 keys, 원문의 비번역 메타데이터를 조합
- 아직 한 번도 번역되지 않은 새 문서는 번역 모드에서 제외
- 변경분 번역이 대기 중인 기존 문서는 마지막 성공 번역을 계속 제공
- 프로젝트의 `syncMode`가 원문이면 원문, 번역이면 번역본을 사용
- `JSON 내보내기`는 현재 동기화 모드의 결과를 프로젝트 루트 `risu_lorebook.json`에 원자적으로 덮어쓰기
- JSON 파일은 명령이나 버튼을 눌렀을 때만 생성

공통 스냅샷 계약은 다음 두 상태를 가집니다.

```ts
type LorebookSnapshot =
  | {
      schema: 1;
      status: "ready";
      project: { id: string; name: string };
      mode: "original" | "translated";
      hash: string;
      lorebook: { type: "risu"; ver: 1; data: RisuEntry[] };
    }
  | {
      schema: 1;
      status: "no-active-project";
      hash: string;
    };
```

로컬 서버:

- `GET /v1/snapshot`
- `If-None-Match`가 현재 hash와 같으면 `304`
- 다르면 완성된 스냅샷과 `ETag` 반환
- 프로젝트 밖이면 `no-active-project` 스냅샷 반환
- localhost 바인딩과 CORS 지원
- JSON 파일을 읽지 않고 메모리에서 즉시 컴파일

릴레이 Worker:

- 저장소의 `relay/`에 Cloudflare Worker 소스 포함
- `PUT /v1/channels/:channel/snapshot`: Obsidian이 현재 스냅샷 업로드
- `GET /v1/channels/:channel/snapshot`: RisuAI가 ETag 기반 polling
- Cloudflare KV에 채널별 최신 스냅샷 저장
- 토큰이 비어 있으면 무인증, 설정되어 있으면 push와 pull 모두 같은 Bearer 토큰 요구
- 릴레이 채널에는 현재 프로젝트 하나만 유지
- 프로젝트 밖 문서로 전환하면 `no-active-project` 상태 업로드
- 컴파일된 hash가 바뀌었을 때 1초 debounce 후 자동 push
- 데스크톱은 로컬 서버와 릴레이를 각각 켤 수 있고, 모바일에서는 로컬 서버 설정을 숨기고 릴레이만 사용
- RisuAI 클라이언트 구현은 포함하지 않고 로컬/릴레이 API 계약과 호출 예시만 문서화
- Worker 배포 명령은 제공하되 자동 실행하거나 실제 원격에 배포하지 않음

### 명령 팔레트

사용자 작업 명령은 다음 6개로 제한합니다.

- `Scriptorium: 대시보드 열기`
- `Scriptorium: 프로젝트 등록`
- `Scriptorium: 번역 실행`
- `Scriptorium: JSON 내보내기`
- `Scriptorium: 릴레이에 지금 동기화`
- `Scriptorium: 작업 취소`

캐시 삭제, 재검사, 프로젝트 제거 같은 유지보수 기능은 대시보드의 고급 도구에서만 제공합니다.

## 코드와 개발 구성

소스는 필요한 수준에서만 모듈화합니다.

```text
src/
  main.ts
  modules/
    project.ts
    lorebook.ts
    sync.ts
    translation/
      planner.ts
      runner.ts
      cache.ts
  ui/
    dashboard.ts
    settings.ts
  shared/
    types.ts
    markdown.ts

relay/
scripts/
  deploy.mjs
```

- npm, TypeScript, esbuild 사용
- `npm run build`: `dist/`에 `main.js`, `manifest.json`, `styles.css` 생성
- `deploy.local.json`: 실제 Obsidian 플러그인 경로 저장, Git에서 제외
- `deploy.local.example.json`: 형식만 저장소에 포함
- `npm run deploy`: 먼저 빌드한 뒤 세 산출물만 대상 폴더로 복사
- `data.json`과 내부 캐시 디렉터리는 절대로 복사·삭제·덮어쓰지 않음
- 현재 로컬 Git 저장소에는 remote를 설정하거나 push하지 않음
- 릴레이 배포는 플러그인 로컬 deploy와 완전히 별도 명령으로 유지

## 기존 버전 이행

- v1 `data.json`의 workspace 목록을 새 project 목록으로 변환
- legacy `translationPrompt`가 있으면 전역 프롬프트로 채택
- 없으면 프로젝트들의 `prompt.md`를 비교해 하나뿐인 고유 내용을 채택
- 서로 다른 prompt가 여러 개면 이전 활성 프로젝트의 prompt를 채택하고 나머지는 경고로 표시
- `risuignore.md` 패턴은 프로젝트 설정으로 가져오기
- 기존 원문과 `translate/` 문단을 대응시켜 현재 상태를 초기 번역 기준으로 채택
- 대응되지 않는 원문 블록은 번역 대기, 대응되지 않는 번역 블록은 충돌 또는 고아 상태로 표시
- 기존 `prompt.md`, `risuignore.md`, `translation_cache.json`은 자동 삭제하지 않고 기본 제외 목록에 넣기
- 구형 번역 캐시는 문단 캐시로 사용하지 않음

## 검증 계획

자동 테스트는 핵심 순수 로직에만 둡니다.

- Markdown 블록 분리: frontmatter, 제목, 문단, 목록, 표, 인용문, 코드 블록
- 문단 diff: 수정, 삽입, 삭제, 이동, 분할·병합
- 수동 번역 수정과 3-way 충돌 판정
- 선택 상태 보존 및 `선택 우선, 없으면 전체` 실행
- Risu 컴파일: 기본 메타데이터, keys 병합, 제외 규칙, 원문/번역 모드
- 스냅샷 ETag: `200`, `304`, 프로젝트 없음, 선택형 Bearer 인증
- v1 프로젝트와 기존 번역본 채택
- 배포 스크립트가 산출물만 복사하고 `data.json`을 보존하는지 확인

실제 Vault QA 완료 조건:

- 원문 문단 하나를 바꾸면 해당 문단 하나만 변경 목록에 나타남
- 선택 번역 후 다른 번역 문단과 수동 수정이 그대로 유지됨
- 활성 문서를 프로젝트 밖으로 옮기면 대시보드·로컬 서버·릴레이가 프로젝트 없음 상태가 됨
- JSON 파일 없이 로컬 Risu 동기화가 동작함
- 모바일에서는 릴레이 경로만으로 동일한 스냅샷을 제공함
- 프로젝트 폴더에 새로운 prompt/cache/ignore 설정 파일이 생기지 않음
